export const metadata = {
  title: '$2000/Month with Free AI Tools - Complete Roadmap',
  description: 'A detailed, precision plan to achieve $2000 per month using 100% free AI tools',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif' }}>
        {children}
      </body>
    </html>
  )
}
